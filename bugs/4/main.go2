package main

contract Advanced(T) {
	T map[string]int, chan int, []string
}

func Len(type T Advanced(T))(t T) {}

func main() {
	a := map[string]int{}
	b := make(chan int)
	c := []string{}

	Len(a)
	Len(b)
	Len(c)
}